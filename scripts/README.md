# Scripts

- `generate_synthetic_data.py` generates sample CSVs for local demos.
- `run_full_pipeline.py` runs the full forecasting and optimization pipeline.
- `run_api.py` starts the Flask API in `src/api/routes.py`.
